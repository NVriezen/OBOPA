
#include "NoRules.h"

int NoRules::RulesCheck(int* neighbours, int points)
{
	//int newCell = points;

	/*
	if (points == 0) {
		if (*neighbours == 3) { //Condition for reproduction
			return 1;
		}
		return 0;
	}
	else if (*neighbours > 3 || *neighbours < 2) {
		return 0;
	}
	else {
		return 1;
	}
	*/
	
	return points; //Return new value of cell
}